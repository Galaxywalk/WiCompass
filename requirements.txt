# =======================
# WiCompass Requirements
# =======================
# Installation order:
#   1. pip install --no-build-isolation chumpy
#   2. pip install -r requirements.txt
#   3. pip install -e .

# =======================
# 3D Body Modeling
# =======================
# human_body_prior depends on chumpy, which must be installed first
git+https://github.com/nghorbani/human_body_prior.git
smplx>=0.1.28
transforms3d>=0.4.2

# =======================
# Core Dependencies
# =======================
torch>=2.0.0
torchvision>=0.15.0
numpy>=2.0.0
scipy>=1.10.1

# =======================
# GPU-accelerated Similarity Search
# =======================
# faiss-gpu-cu12: For CUDA 12.x (recommended for PyTorch 2.x)
# Requires numpy>=2.0.0 and CUDA 12.x compatible GPU driver
faiss-gpu-cu12>=1.13.0

# =======================
# Data Processing
# =======================
h5py>=3.11.0
pandas>=2.0.0
PyYAML>=6.0
tqdm>=4.60.0

# =======================
# Machine Learning & Visualization
# =======================
scikit-learn>=1.0.0
matplotlib>=3.5.0
seaborn>=0.12.0
tensorboard>=2.10.0
einops>=0.6.0
pillow>=10.0.0

# =======================
# Jupyter Notebook Support
# =======================
ipykernel>=7.0.0
jupyterlab>=4.0.0

# =======================
# Utilities
# =======================
ray>=2.0.0
loguru>=0.7.0
omegaconf>=2.3.0

# =======================
# Optional Dependencies
# =======================
# plotly>=5.0.0       # Interactive visualization (optional)
